/** @format */
import { components } from '../index';
import Vue from 'vue';
console.log(Vue);
export const config = {
  tagPrefix: 'item-',
  tags: components
};
// el-form 配置
export const formConfig = {};
